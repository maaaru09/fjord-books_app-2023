<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to Book.human_attribute_name(:edit_book), edit_book_path(@book) %> |
  <%= link_to Book.human_attribute_name(:back_books), books_path %>

  <%= button_to Book.human_attribute_name(:destroy_book), @book, method: :delete %>
</nav>
